# The builder stage installs node_modules on alpine and then runs `COPY . .`.
# Without these exclusions a host install leaks in and overwrites it — a Windows
# or macOS tree carries the wrong @next/swc binary and the container build fails.
node_modules
.next
tsconfig.tsbuildinfo
Dockerfile
.dockerignore
