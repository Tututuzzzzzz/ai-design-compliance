# Optional: local OCR engine with bounding boxes (offline, ~80MB).
# Install with:  pip install -r requirements-ocr.txt
# Without this, the pipeline falls back to Vision-LLM OCR (still returns boxes).
rapidocr-onnxruntime==1.4.4
opencv-python-headless==4.10.0.84
numpy==2.2.1
